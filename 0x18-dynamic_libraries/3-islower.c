#include "main.h"

/**
 * _islower -> check if the character is a lowercase
 * @c: a character argument
 * Return: returns 1 aind 0 depending on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
